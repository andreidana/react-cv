import positions from '../static/positions';

import styles from './full-stack-developer.module.scss';

const FullStackDeveloper = () => (
  <>
    <div className={styles.FullStackDeveloperStyle}>
      <p>{positions.fullStackDeveloper}</p>
    </div>
  </>
);

export default FullStackDeveloper;
